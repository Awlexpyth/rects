import pgzrun
import random
import pygame

#flappy ball

# Pygame Standard for deciding the title of your game window
TITLE="Flappy ball:1"
# Pygame Standard for deciding the width and height for your game window in pixels
WIDTH=1000
HEIGHT=800
#variables to set up random colors
red=random.randint(0,255)
green=random.randint(0,255)
blue=random.randint(0,255)
clr=(red,green,blue)
#screen=pygame.display.set_mode((800,800))

#BLUE = 0, 128, 255
GRAVITY = 2000.0  

# pixels per second per second
#Create a class for Ball
class ball:
    def __init__(self,initial_x,initial_y):
        self.x=initial_x
        self.y=initial_y
        self.vx=200
        self.vy=0
        self.radius= 40
        print("alex is inside the constructor")
    def recty(self):
                
                screen.draw.filled_circle((self.x,self.y),self.radius,clr)
        #screen.draw.rect(self.screen1,(self.x,self.y),self.radius,clr)
arectangle=ball(110,110)





        
        



#Create the instance of the ball


def draw():
    screen.clear()
    arectangle.recty()
    

#def update(dt):
    # Apply constant acceleration formulae
    
    # detect and handle bounce
    
    # X component doesn't have acceleration
    
#def on_key_down(key):
    """Pressing a key will kick the ball upwards."""

pgzrun.go()
